<% provide(:title, @component.title) %>

<template-show :template="<%= {
  contents: TemplateParser.new(type: 'components', category: @component.category, template: @component).contents,
  cssPath: Webpacker.manifest.lookup('application.css'),
  goBackURL: category_url(@component.category)
}.to_json %>"></template-show>
